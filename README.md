# gtm-cowork-vs-code

A B2B Fusion site by **Jon Russo**, hosted on Vercel with auto-deploy from this repo.

> **Status:** work in progress. The page was seeded from an existing design template
> as a starting structure. The copy will be replaced wholesale with B2B Fusion / Jon
> Russo content (no third-party author or AI-institute references).

## Contents

- `index.html` — the page (self-contained; all CSS inline)
- `abn-logo-dark.png` — placeholder image asset (to be replaced)

## Editing & deploying

Edit `index.html`, then push to `main` — Vercel redeploys automatically.

```bash
git add -A && git commit -m "your message" && git push origin main
```

Preview locally:

```bash
python3 -m http.server 8000   # then visit http://localhost:8000
```
